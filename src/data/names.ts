import data from "./names_data.json";

// todo: is this file even necessary or can you just import the json directly
export const firstNames = data.firstNames;
export const lastNames = data.lastNames;
export const rituals = data.rituals;
